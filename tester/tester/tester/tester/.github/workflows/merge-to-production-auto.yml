name: 08 Auto Merge to Production

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  merge:
    if: github.repository == 'pentasoftwareconsultancy/book-my-parcel-backend' && github.event.pull_request.head.ref == 'stage'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auto-merge PR to Production
        run: |
          echo "⏳ Waiting 10 seconds before auto-merge..."
          sleep 10
          
          curl -X PUT \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/pentasoftwareconsultancy/book-my-parcel-backend/pulls/${{ github.event.pull_request.number }}/merge \
            -d '{
              "commit_title": "Merge staging → main: Production release",
              "commit_message": "Auto-merged from staging branch to production",
              "merge_method": "squash"
            }'
