name: 06 Deploy Staging

on:
  push:
    branches:
      - stage
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'

jobs:
  deploy:
    if: github.repository == 'pentasoftwareconsultancy/book-my-parcel-backend'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: stage

      - name: Deploy Staging to Render
        run: |
          echo "🚀 Triggering Render deployment for staging..."
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_STAGING }}"
          echo "✅ Staging deployment triggered successfully"
